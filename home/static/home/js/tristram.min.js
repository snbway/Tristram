/*! This is uglify test - 2015-09-08 */$(function() {
    $(".partner img").hover(function() {
        var src = $(this).attr("src");
        src = src.replace(".png", "_hover.png"), $(this).attr("src", src);
    }, function() {
        var src = $(this).attr("src");
        src = src.replace("_hover.png", ".png"), $(this).attr("src", src);
    }), $(window).bind("scroll", function() {
        var $self = $(this), $back = $(".back");
        $self.scrollTop() > 300 ? $back.show() : $back.hide();
    }), $(".back").bind("click", function() {
        window.scrollTo(0, 0);
    });
});